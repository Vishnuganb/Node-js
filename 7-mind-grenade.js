const num1 = 5
const num2 = 10

function addValues(){

    console.log(`The sum is : ${num1 + num2}`)

}

addValues()

// node is export this one
// it actually wraps it in the function
//behind the scene module will do these things